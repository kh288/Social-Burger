<div class="container">
    <div class="row">
        {{#if ""}}
        <div class="container justify-content-center">
            {{#each burgers as burger}}
            {{>burgerPost}}
            {{/each}}
        </div>
        {{/if}}
    </div>
</div>