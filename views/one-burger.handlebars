


<div class="card">
    <div class="card-header  flex-row justify-space-between">
            <h2>
            <a href='/burger/{{burger_id}}'>
                <div class="col-8">{{burger.title}}</div>
            </a>
            </h2>
            <div class="col-3">posted by {{burger.user.username}} on {{format_date burger.date_created}}
            </div>
    </div>  
        <p class="card-body">
        {{burger.ingredients}}
        </p>
</div>
        <div class="card">
            <input class="burgerId-input" type="hidden" name="burgerId-input" value="{{burger.user_id}}"/>
            <div class="card-header"></div>
            <form class="card-body commentForm">
                {{!-- <label for="burger_id" class="form-label"></label> --}}
                {{!-- <input type="text"  class="burgerId-input" value="{{burger.burger_id}}"/> --}}
                <label for="comment" class="form-label"></label>
                <input type="text" class="form-input comment-input">
                <button type="submit" class="btn btn-primary signupBtn">submit</button>
            
        
            </form>
                {{!-- <div class="comments">
                {{#each this}}
                <div>
                    <p class="card-body">
                    {{text}} - {{user.username}} {{format_date date_created}}
                    </p>
                </div>
                {{/each}}
                </div> --}}

                {{> comments burger.comments}}
        </div>
        

    <script src="/js/comment.js"></script>