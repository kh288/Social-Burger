<div class="card">
    <div class="card-header  flex-row justify-space-between">
            <h2>
            <a href=''>
                <div class="col-8">{{burger.title}}</div>
            </a>
            </h2>
            <div class="col-3">posted by {{burger.user.username}} on {{format_date burger.date_created}}
            </div>
    </div>  
        <p class="card-body">
        {{burger.ingredients}}
        </p>
</div>
    <input type="hidden" name="post-id" value="{{burger.id}}"/>
            <div class="card">
            <div class="card-header"></div>
            <form class="card-body" id="new-comment-form">
                <p class="form-label">Comment:</p>
                <textarea class="form-input" id="comment-body" name="comment-body"></textarea>
                <button type="submit" class="btn btn-primary">Submit:</button>
            </form>
    {{> comments burger.comments}}
    </div>