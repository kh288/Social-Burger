<div class="card">
    <div class="card-header  flex-row justify-space-between">
            <h2>
            <a href='/burger/{{burger_id}}'>
                <div class="col-8">{{burger.title}}</div>
            </a>
            </h2>
            <div class="col-3">posted by {{burger.user.username}} on {{format_date burger.date_created}}
            </div>
    </div>  
        <p class="card-body">
        {{burger.ingredients}}
        </p>
</div>
        <div class="card">
            <div class="card-header"></div>
            <form class="card-body" id="new-comment-form">
                <input type="hidden"  class="burgerId" name="burger-id" value="{{burger.burger_id}}"/>
                <p class="form-label">Comment:</p>
                <textarea class="form-input commentInput" name="comment-body"></textarea>
                <button type="submit" class="btn btn-primary">Submit:</button>
            </form>
                {{!-- <div class="comments">
                {{#each this}}
                <div>
                    <p class="card-body">
                    {{text}} - {{user.username}} {{format_date date_created}}
                    </p>
                </div>
                {{/each}}
                </div> --}}

                {{> comments burger.comments}}
        </div>

    <script src="/js/comment.js"></script>